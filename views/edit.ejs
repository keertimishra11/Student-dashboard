<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Student</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">

  <h2 class="text-center mb-4">✏ Edit Student</h2>

  <form action="/update/<%= student.id %>" method="POST" class="card p-4 shadow">
    <div class="mb-3">
      <label>Name</label>
      <input type="text" name="name" class="form-control" value="<%= student.name %>" required>
    </div>

    <div class="mb-3">
      <label>Email</label>
      <input type="email" name="email" class="form-control" value="<%= student.email %>" required>
    </div>

    <div class="mb-3">
      <label>Course</label>
      <select name="course" class="form-control" required>
        <option value="Math" <%= student.course === 'Math' ? 'selected' : '' %>>Math</option>
        <option value="Science" <%= student.course === 'Science' ? 'selected' : '' %>>Science</option>
        <option value="English" <%= student.course === 'English' ? 'selected' : '' %>>English</option>
        <option value="Computer" <%= student.course === 'Computer' ? 'selected' : '' %>>Computer</option>
        <option value="History" <%= student.course === 'History' ? 'selected' : '' %>>History</option>
      </select>
    </div>

    <div class="mb-3">
      <label>Marks</label>
      <input type="number" name="marks" class="form-control" min="0" max="100" value="<%= student.marks %>" required>
    </div>

    <button type="submit" class="btn btn-primary">✅ Update Student</button>
    <a href="/students-list" class="btn btn-secondary">⬅ Back</a>
  </form>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
